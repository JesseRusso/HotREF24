<UserControl x:Class="HotPort.Views.DirectoriesSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Default template directory" Padding="0"/>
        <TextBox Grid.Row="1" Grid.Column="0" Height="22" VerticalContentAlignment="Center"
                 Text="{Binding TemplateDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Browse…" Width="75" Height="22" Margin="8,0,0,0"
                Command="{Binding BrowseTemplateDirCommand}"/>

        <Label Grid.Row="2" Grid.ColumnSpan="2" Content="Code library directory" Padding="0" Margin="0,12,0,0"/>
        <TextBox Grid.Row="3" Grid.Column="0" Height="22" VerticalContentAlignment="Center"
                 Text="{Binding CodeLibDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="3" Grid.Column="1" Content="Browse…" Width="75" Height="22" Margin="8,0,0,0"
                Command="{Binding BrowseCodeLibDirCommand}"/>
    </Grid>
</UserControl>
