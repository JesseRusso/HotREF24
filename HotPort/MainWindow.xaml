<Window x:Class="HotPort.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:HotPort.Properties"
        xmlns:viewModels="clr-namespace:HotPort.ViewModels"
        mc:Ignorable="d"
        Title="HotREF" Height="400" Width="360" WindowStyle="SingleBorderWindow" Closing="Window_Closing" ResizeMode="NoResize">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>
    <Grid x:Name="grid" HorizontalAlignment="Center" Width="360" Margin="0,0,0,0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.8*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button x:Name="WorksheetButton" Content="Select Worksheet" HorizontalAlignment="Center" Margin="0,35,0,0" VerticalAlignment="Top" Width="320" Height="20" Command="{Binding SelectWorksheetCommand}"/>
        <Button x:Name="TemplateButton" Content="Select Template" HorizontalAlignment="Left" Margin="20,31,0,0" VerticalAlignment="Top" Width="190" Grid.Row="1" Height="20" Command="{Binding SelectTemplateCommand}"/>
        <Button x:Name="CreatePropButton" Content="Create Proposed File" HorizontalAlignment="Center" Margin="0,99,0,0" VerticalAlignment="Top" Width="320" Height="20" Grid.Row="1" Command="{Binding CreateProposedFileCommand}"/>
        <CheckBox x:Name="WindowCheckbox" Content="Include Windows" Grid.Row="1" Margin="230,35,10,76" IsChecked="{Binding Source={x:Static properties:Settings.Default},Path=WindowsCheckbox, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button x:Name="CreateRefButton" Content="Create REF" HorizontalAlignment="Center" Width="320" Grid.Row="2" Height="20" VerticalAlignment="Top" Command="{Binding CreateRefCommand}" Margin="0,120,0,0"/>
        <TextBlock x:Name="worksheetTextBlock" HorizontalAlignment="Left" Margin="20,60,0,0" TextWrapping="Wrap" Text="{Binding WorksheetText}" VerticalAlignment="Top" Width="190" RenderTransformOrigin="0.5,0.692"/>
        <Button x:Name="SelectPropFileButton" Content="Select Proposed File" HorizontalAlignment="Left" Margin="20,50,0,0" VerticalAlignment="Top" Width="240" Height="20" Grid.Row="2" Command="{Binding SelectProposedFileCommand}"/>
        <TextBlock x:Name="ProposedFileTextBlock" HorizontalAlignment="Left" Margin="20,0,0,0" Grid.Row="2" TextWrapping="Wrap" VerticalAlignment="Center" Width="240" Text="{Binding ProposedFileText}"/>
        <ComboBox x:Name="ZoneSelectBox" ItemsSource="{Binding Zones}" SelectedIndex="{Binding SelectedZoneIndex}" HorizontalAlignment="Right" Margin="0,50,20,0" Grid.Row="2" VerticalAlignment="Top" Width="75" Height="20" />
        <Menu Grid.Column="0" Grid.Row="0" Height="20" Width="{Binding Width, ElementName=grid}" VerticalAlignment="Top">
            <MenuItem Header="Settings" HorizontalAlignment="Left" Grid.IsSharedSizeScope="True" Margin="15,0,0,0">
                <MenuItem x:Name="DefaultDirectory" Header="Default template directory" Command="{Binding SetDefaultDirectoryCommand}" HorizontalAlignment="Left" MaxHeight="20"     ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
            </MenuItem>
        </Menu>
        <GroupBox Header="PROPOSED" Margin="10,10,10,0" Grid.Row="1" BorderBrush="#FF979CA0" Panel.ZIndex="-1">
            <TextBlock x:Name="TemplateTextBlock" HorizontalAlignment="Left" Margin="5,30,0,0" TextWrapping="Wrap" VerticalAlignment="Top" RenderTransformOrigin="0.487,1.646" Width="300" Text="{Binding TemplateText}"/>
        </GroupBox>
        <GroupBox Header="REFERENCE" Margin="10,134,10,10" Grid.Row="1" Panel.ZIndex="-1" BorderBrush="#FF979CA0" Grid.RowSpan="2">
            <Label Content="Zone" HorizontalAlignment="Left" Margin="250,0,0,0" VerticalAlignment="Top" Width="75" Height="27"/>
        </GroupBox>
    </Grid>
</Window>
